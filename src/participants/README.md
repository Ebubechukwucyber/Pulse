Participant skeletons live here for the live Mozaik pass.

Frozen ids: sentry, triage, archaeologist, hypothesis, fixer, redteam, comms, wall, commander.

Do not implement live inference until `npm run replay` is confirmed working.
Each class should extend Mozaik `BaseParticipant` and forward domain events onto `src/bus.ts`.
